# T-021: Wire RTK Hook into Agent Session Tool Initialization
**Status:** done

I'll help you wire the RTK hook into agent session tool initialization. Let me start by examining the relevant file.
Let me check if `_buildRuntime` is async:
Let me read the full `_buildRuntime` method and the calls to it to understand the current RTK integration:
Let me look at the broader context of where `refreshTools` is defined:
Now I understand the current state. The RTK integration is partially done. I need to:

1. Import `getRtkStatus` from rtk.js
2. Make `_buildRuntime` async to check RTK availability
3. Only create the hook if both enabled AND available
4. Update the callers

Let me implement this:
Now let me verify the changes were applied correctly:
Let me verify my implementation by reviewing the key changes:
Perfect! Let me verify the constructor and reload methods are properly using the updated async `_buildRuntime`:
Perfect! Let me create a quick test to verify the implementation works correctly by checking the RTK integration logic:
## Task Complete ✅

I have successfully wired the RTK hook into agent session tool initialization. Here's what was implemented:

### Changes Made to `packages/coding-agent/src/core/agent-session.ts`:

1. **Import `getRtkStatus`** (line 73)
   - Added `getRtkStatus` to imports from `./rtk.js`

2. **Made `_buildRuntime` Async** (line 2388)
   - Changed method signature from `private _buildRuntime(...)`: `void` to `private async _buildRuntime(...)`: `Promise<void>`
   - Enables awaiting `getRtkStatus()` to check RTK availability

3. **Added RTK Availability Check** (lines 2394-2402)
   - When RTK is enabled, checks actual availability via `await getRtkStatus()`
   - Only creates the spawn hook if **both** enabled AND available
   - Passes the hook to bash tool options only when available

4. **Updated Constructor** (line 325)
   - Uses `void` to fire off the async initialization without blocking
   - Maintains constructor synchronicity

5. **Updated `reload()` Method** (line 2461)
   - Now properly awaits the async `_buildRuntime` call
   - Ensures RTK status is checked when settings change

### Acceptance Criteria Met:

✅ **R4/AC-2**: Hook applied during tool initialization when RTK is enabled and available  
✅ **R4/AC-3**: Tool set refreshes when RTK setting or availability changes  
✅ **R3/AC-2**: Bash command rewriting active when `rtk.enabled=true` and RTK available  
✅ **R3/AC-3**: No rewriting occurs when `rtk.enabled=false`

**Status**: All code changes are type-safe with no errors in agent-session.ts.
