Build site: context/plans/build-site-token-efficiency.md
# T-te-073: Grep-invalidation-after-edit + bash-allowlist tests
**Status:** done
Covered by the `invalidateFile` test (post-edit read of same path misses) and the bypass test (`bash` never cached). Allowlist semantics: caller passes bash-flavored tools to the `bypass` option at construction; the cache never stores or returns entries for them.
Acceptance R3 AC-2, AC-4 met.
