Build site: context/plans/build-site-token-efficiency.md
# T-te-075: Call-site cannot force caching for bypassed tools
**Status:** done
`put()` unconditionally returns early when `bypass.has(tool)`. There is no override path from the call site to force caching.
Test: "bypassed tools are never cached" asserts `get()` returns undefined after a `put`.
Acceptance R4 AC-3 met.
