Build site: context/plans/build-site-token-efficiency.md
# T-te-052: tools/system/project layer bytes unchanged across a trim
**Status:** done
`trimMiddle()` only operates on `Turn[]` (message history). It does not touch the `tools/system/project` layers produced by `assembleLayered()`. R1 layer-isolation invariant (T-003) guarantees the upstream bytes cannot drift.
Acceptance R5 AC-2 met.
