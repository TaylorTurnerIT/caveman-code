Build site: context/plans/build-site-token-efficiency.md
# T-te-046: cached + uncached = total invariant
**Status:** done
`totalInputTokens(u)` returns `cached + uncached`. Test: "cached + uncached equals total input".
Acceptance R8 AC-3 met.
