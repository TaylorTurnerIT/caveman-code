Build site: context/plans/build-site-token-efficiency.md
# T-te-047: Per-task CacheRetention at agent-loop call site with role defaults
**Status:** done
`resolveRetention({ roleDefault, cliFlag?, cavekitPhaseOverride? })` composes the role default with the CLI flag and phase override. Tests cover all precedence paths.
Acceptance R3 AC-1, AC-2 met.
