# T-019: RTK Command Rewrite Function
**Status:** done

Implemented.

Files:
- `packages/coding-agent/src/core/rtk.ts`
- `packages/coding-agent/test/rtk.test.ts`
- `packages/cavekit-extension/src/rtk-exec.ts`

What changed:
- `rewriteCommand(command: string): Promise<string>` now uses `child_process.execFile`
- 200ms timeout on `rtk rewrite`
- fail-open on:
  - non-zero exit
  - `ENOENT`
  - timeout
  - synchronous `execFile` failure
- no double rewrite for `rtk` / `rtk ...`
- compound commands passed through unchanged as single argument
- `createRtkSpawnHook()` now awaits rewrite and skips rewrite call when RTK unavailable
- extension-side RTK exec updated to cache async RTK availability for sync shell exec path

Validation:
- `cd packages/coding-agent && node ../../node_modules/vitest/vitest.mjs --run test/rtk.test.ts`
- `npm run check`

Acceptance criteria:
- R2/AC-1: met
- R2/AC-2: met
- R2/AC-3: met
- R2/AC-4: met
- R2/AC-5: met
- R2/AC-6: met
- R2/AC-7: met via 200ms timeout + local `execFile` path
- R2/AC-8: met
  - disabled: no RTK hook used
  - unavailable: hook checks RTK status first, skips `rtk rewrite`, returns original command

Notes:
- Added/updated tests for success, non-zero exit, timeout, spawn failure, double-rewrite guard, compound commands, empty stdout, hook integration, unavailable RTK skip.
