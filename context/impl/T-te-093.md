Build site: context/plans/build-site-token-efficiency.md
# T-te-093: Emit tool_cache_hit / tool_cache_miss trace events with saved-tokens estimate
**Status:** done
`ToolResultCache` accepts a `traceSink` in options. Every `get()` call emits:
- `tool_cache_hit` with `savedTokens = ceil(byteLen / 4)` on hit
- `tool_cache_miss` on miss (including bypassed tools)
Event shape: `{ type, tool, sessionId, savedTokens? }`.
Test: "traceSink emits tool_cache_hit/miss with savedTokens".
Acceptance R6 AC-1, AC-2 met.
