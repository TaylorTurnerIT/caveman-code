Build site: context/plans/build-site-token-efficiency.md
# T-te-074: Allowlist-driven bypass for bash/network/time-dependent tools
**Status:** done
`ToolResultCache({ bypass: ["bash", "fetch", "now"] })`. `isBypass()` check short-circuits both `get()` (emits `tool_cache_miss`) and `put()` (no-op) for allowlisted tools.
Acceptance R4 AC-1, AC-2 met.
