Build site: context/plans/build-site-token-efficiency.md
# T-te-072: Read-cache invalidation on write/edit + bash allowlist semantics
**Status:** done
`ToolResultCache.invalidate(predicate)` and `invalidateFile(touchedFile)` drop entries whose args reference the touched path. Tool dispatch layer calls `invalidateFile(path)` after every successful write/edit/apply_sr_diff/edit_symbol. Bash tools are on the bypass allowlist so they never populate the cache to begin with (T-074 path). Non-allowlisted bash commands that may mutate files trigger a blanket `invalidate(() => true)` in the caller.

Tests: `invalidateFile drops entries whose args reference the touched path`.
Acceptance R3 AC-1, AC-3 met.
