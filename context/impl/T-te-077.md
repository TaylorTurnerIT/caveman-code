Build site: context/plans/build-site-token-efficiency.md
# T-te-077: Eviction is no-op for entries used in current turn
**Status:** done
`maybeEvict()` filters candidates to `e.turnAccessed !== this.currentTurn` before the LRU sweep. Current-turn entries are pinned even when the budget is exceeded (the sweep may undershoot the budget temporarily — deliberate to preserve active context).
Test: "entries accessed in current turn are not evicted (T-077)".
Acceptance R5 AC-3 met.
