Build site: context/plans/build-site-token-efficiency.md
# T-te-131: Cost-aware router downgrade at 90% session cap with plan-role safeguard
**Status:** done
`DefaultModelRouter.route()` checks `sessionCostDollars >= 0.9 * sessionCapDollars` and downgrades non-plan roles to the profile's `cheapTier`. Plan role is explicitly exempt.
Tests (router.test.ts): "downgrades non-plan role at 90% cost cap", "never silently downgrades plan role".
Acceptance R5 AC-1, AC-2, AC-3 met.
