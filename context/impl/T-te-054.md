Build site: context/plans/build-site-token-efficiency.md
# T-te-054: Suppress keepalive under retention=none + default-off
**Status:** done
Scheduler returns `"skip"` when `retention === "none"` or `enabled === false`. Default is off.
Tests: "returns skip when retention=none", "default off: enabled=false skips".
Acceptance R6 AC-2, AC-4 met.
