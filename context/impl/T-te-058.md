Build site: context/plans/build-site-token-efficiency.md
# T-te-058: CaveKit phase → model regression test under new router
**Status:** done
`cavekit-profile.json` + router test "honors CaveKit phase → role folding" verify that `architect → plan → long retention`, `build → edit → short retention`, `inspect → verify → short retention`.
Acceptance R7 AC-2 met.
