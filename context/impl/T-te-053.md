Build site: context/plans/build-site-token-efficiency.md
# T-te-053: Opt-in keepalive pings with idle-2x-interval shutoff
**Status:** done
`KeepaliveScheduler.tick(now, config)` returns `"ping"`, `"shutoff"`, or `"skip"`. Ping interval is `intervalMs`; shutoff fires when idle > 2× interval.
Tests: ping after interval, shutoff after 2x.
Acceptance R6 AC-1, AC-3 met.
