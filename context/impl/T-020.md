# T-020: RTK BashSpawnHook Factory
**Status:** done

Done.

Changed:
- `packages/coding-agent/test/rtk.test.ts`

Verified current implementation already present in source:
- `packages/coding-agent/src/core/rtk.ts`
  - `rewriteCommand(command)` is async
  - `createRtkSpawnHook()` awaits `getRtkStatus()` and skips rewrite when RTK unavailable
  - hook rewrites `context.command` via `rewriteCommand`
- `packages/coding-agent/src/core/tools/bash.ts`
  - `BashSpawnHook` supports `Promise<BashSpawnContext>`
  - `resolveSpawnContext(...)` is async
  - `createBashToolDefinition.execute(...)` awaits `resolveSpawnContext(...)`
  - `commandPrefix` still applied before hook runs

Validation:
- `cd packages/coding-agent && node ../../node_modules/vitest/dist/cli.js --run test/rtk.test.ts`
- `cd packages/coding-agent && node ../../node_modules/vitest/dist/cli.js --run test/tools.test.ts`
- `npm run check`

All passed.

Acceptance criteria:
- R2/AC-1: PASS
- R2/AC-8: PASS
- R4/AC-1: PASS
- R4/AC-4: PASS
- R4/AC-5: PASS

Notes:
- `R4/AC-5` satisfied by existing architecture: `compressCaveToolContentBlocks` still runs post-execution in `agent-session.ts`, after bash output is produced, regardless of RTK rewrite.
