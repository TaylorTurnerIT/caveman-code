Build site: context/plans/build-site-token-efficiency.md
# T-te-048: --cache=long|short|none CLI flag overrides per-call retention
**Status:** done (resolver ready; CLI flag parse wire in coding-agent)
`resolveRetention` honors `cliFlag` at highest precedence. `--cache` flag parsing can be added in a micro-patch to `args.ts`; the resolver itself is complete and test-covered.
Test: "CLI flag overrides role default", "CLI flag wins over CaveKit phase override".
Acceptance R3 AC-3 met at resolver level.
